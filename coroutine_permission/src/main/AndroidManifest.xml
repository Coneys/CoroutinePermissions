<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.devstruktor.coroutinePermission">
    
    <application>

        <provider android:authorities="${applicationId}.PermissionsInitProvider"
                  android:name="com.devstruktor.initalizer.PermissionsInitProvider"/>

        <activity android:name="com.devstruktor.nabinbhandariPermissions.PermissionsActivity"
                  android:theme="@style/Permissions.TransparentTheme"
                />
    </application>
    
</manifest>
